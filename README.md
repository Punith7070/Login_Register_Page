# Login_Register_Page
Login/Register page Html, Css, Java, Sql.
This project is created using Eclipse enterprise edition. So it should be run on that.
It uses mysql server for database to store username and passwords. Check for the mysql server userId and password. Here it is set as root for both.
You should add tomcat server in eclipse to run this.
To run project, run index.html file in webapp folder .
